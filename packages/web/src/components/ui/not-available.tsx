// The canonical "no value" affordance, so screens stop scattering
// `<span className="text-muted-foreground">n/a</span>`.
export function NotAvailable() {
	return <span className="text-muted-foreground">n/a</span>;
}
