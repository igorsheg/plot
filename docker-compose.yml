services:
  plot:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./workspaces:/app/workspaces
      - ./WORKFLOW.md:/app/WORKFLOW.md
    environment:
      - PLOT_WORKSPACE_ROOT=/app/workspaces
